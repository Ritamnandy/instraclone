import 'package:flutter/material.dart';

class Sharescreen extends StatelessWidget {
  const Sharescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('share page')));
  }
}
